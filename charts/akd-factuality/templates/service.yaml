apiVersion: v1
kind: Service
metadata:
  name: {{ include "akd-factuality.fullname" . }}
  labels:
    {{- include "akd-factuality.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    {{- include "akd-factuality.labels" . | nindent 4 }}
